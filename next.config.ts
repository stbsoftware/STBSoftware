import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Oculta el botón "N" de la esquina en desarrollo (indicador de ruta / dev tools). */
  devIndicators: false,
};

export default nextConfig;
